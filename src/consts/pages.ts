export enum PagesName {
	Home = "Главная",
	MyAlbums = "Мои альбомы",
}

export enum PagesPath {
	Home = "/",
	Album = "/album/:id",
	MyAlbums = "/my-albums",
	Search = "/search/:query",
	Error = "*",
}
