import styled from "styled-components";

const AuthWrapper = styled.div`
	width: 400px;
	margin: 0 auto;
	padding: 2rem;
	/* 
	border: 2px solid ${({ theme }) => theme.colors.primary.neutral};
	box-shadow: 0px 0px 15px 5px ${({ theme }) => theme.colors.primary.light}; */
	/* border-radius: ${({ theme }) => theme.sizes.borderRadius}; */
`;
export { AuthWrapper };
