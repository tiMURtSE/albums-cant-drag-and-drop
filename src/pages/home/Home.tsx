import React from 'react';
import Navbar from '../../components/layout/navigation/Navbar';

type Props = {};

const Home = (props: Props) => {
    return (
        <>
            <Navbar />
        </>
    );
};

export default Home;